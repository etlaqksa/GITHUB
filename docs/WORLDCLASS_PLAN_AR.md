# خطة تنفيذ مرحلية (مقترحة)

## المرحلة 1 (منجزة في النسخة الحالية)
- استقرار BlogPost (fix hooks + تحسينات الربط + الصور).
- تحسين SEO الأساس (Canonical + OG + Schema على الصفحات الأساسية).
- PWA + caching + security headers.

## المرحلة 2 (1-2 أسبوع)
- Lighthouse Optimization: تقليل JS، تحسين الخطوط (self-hosting)، ضغط الصور وتحويلها WebP/AVIF.
- Schema متقدم لكل خدمة/دراسة حالة (Service + FAQ + Breadcrumb).
- تحسين محتوى المقالات ليصبح أقل تكراراً وأكثر قرباً للعميل.

## المرحلة 3 (2-4 أسابيع)
- مساعد ذكي حقيقي (RAG) عبر Netlify Functions + مزود LLM (OpenAI) + قاعدة معارف.
- لوحات مراقبة أداء (Web Vitals) + تنبيهات.
